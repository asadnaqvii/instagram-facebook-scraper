# ── Core scraper ───────────────────────────────────────────────
playwright>=1.40.0         # CDP browser automation
pydantic>=2.0.0            # data models
rich>=13.0.0              # CLI output
python-dotenv>=1.0.0      # .env config loading
yt-dlp>=2024.0.0          # media download

# ── Web dashboard + JSON API ───────────────────────────────────
flask>=3.0.0              # dashboard + /api/v1
requests>=2.31.0         # Ollama client + example_client.py

# ── MySQL backend (only needed when META_OSINT_DB_BACKEND=mysql) ─
PyMySQL>=1.1.0           # pure-Python MySQL driver
