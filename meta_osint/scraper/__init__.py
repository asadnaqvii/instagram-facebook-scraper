# Extraction layer. Submodules:
#   instagram.py — IG search / hashtag / profile / post / comment extraction
#   facebook.py  — FB search / hashtag / page / post / comment extraction
#   media.py     — yt-dlp metadata + media download (shared)
#   helpers.py   — parsing helpers (counts, hashtags, timestamps)
